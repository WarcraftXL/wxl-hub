<section class="railx divide">
  <div class="sec">
    <h2><%= heading %></h2>
    <span class="spacer"></span>
    <span class="aside"><a href="/store">Browse all <%= total %> <%- icon("arrow-right") %></a></span>
    <div class="sorts">
      <% for _, s in ipairs(sorts) do %>
        <a class="<%= s.key == sort and 'on' or '' %>"
           href="/store?sort=<%= s.key %>"
           hx-get="/store/rail?sort=<%= s.key %>" hx-target="closest section"
           hx-swap="outerHTML"><%= s.label %></a>
      <% end %>
    </div>
    <div class="arrows">
      <button onclick="railScroll(this,-1)" aria-label="Scroll left"><%- icon("chevron-left") %></button>
      <button onclick="railScroll(this,1)" aria-label="Scroll right"><%- icon("chevron-right") %></button>
    </div>
  </div>
  <%- render("grid", { modules = modules }) %>
</section>
